class Solution {
    public boolean containsDuplicate(int[] nums) {
        Set<Integer> times = new HashSet<Integer>();
        for(int num : nums){
            times.add(num);
        }
        if(times.size() == nums.length){
            return false;
        }else{
            return true;
        }
        
    }
}
